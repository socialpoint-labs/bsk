language: go

go:
    - '1.16'
    - '1.17'
    - 'tip'

install:
    - make install-tools

script:
    - make ci-check

after_success:
    - test $TRAVIS_GO_VERSION != 'tip' && bash <(curl -s https://codecov.io/bash)
